# Team placeholders

This folder is intentionally minimal.

- Biruk's required features live in `systems/biruk/feature1` and `systems/biruk/feature2`.
- Other team members can add their own feature folders/packages under `systems/` as needed.
